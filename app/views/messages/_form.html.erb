<%= form_with model: [conversation, message] do |f| %>
  <div class="field">
    <%= f.label :body, "Message" %>
    <%= f.text_area :body, rows: 4, style: "max-width: 100%;" %>
  </div>

  <div class="actions">
    <%= f.submit "Send" %>
  </div>
<% end %>